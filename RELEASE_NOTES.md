# 여의도성당Signage 릴리즈 노트

## 1.2.39 (2026-09-05)

- 화면 보호기의 “화면을 터치해 주세요” 안내를 큰 노란색 버튼으로 변경했습니다. WEB 관리자에서 글자 크기, 좌우·위아래 여백, 배경색과 글자색을 조절할 수 있습니다.
- 툴바의 현재 주제 제목을 누르면 같은 언어의 주제 선택 화면으로 돌아갑니다. 가로·세로 툴바에 모두 적용하며, 이 경로에서는 주제가 하나여도 선택 화면을 표시합니다.
- WEB 관리자 화면 미리보기에서 실제 사이니지의 클릭과 드래그를 조작할 수 있습니다. 화면 배율·여백·다중 모니터 좌표를 보정하고, 관리자 인증과 드래그 입력 순서를 확인합니다. 숨김·최소화 또는 다른 창이 가린 영역은 조작하지 않으며, 연결이 끊기면 마지막 입력 후 3초 안에 마우스 누름을 해제합니다.
- 화면 미리보기에 사이니지 보이기·숨기기 버튼과 실행 중인 버전 표시를 추가했습니다.
- 원격 접속 수동 고정 ID의 `ysignage숫자` 제한을 제거했습니다. `yeouido`, `church-lobby`처럼 영문·숫자·하이픈으로 지정할 수 있고, 중복 시 `-1`, `-2`를 붙여 저장합니다. 기존 ID와 재시작·업데이트·백업 복원 시 장치 ID 보존 동작을 유지합니다.

## 1.2.38 (2026-09-05)

- WEB 관리자에 릴리스 ZIP을 직접 업로드하는 강제 업데이트를 추가했습니다. 인터넷 연결 없이 같은 버전 재설치와 이전 버전 설치를 요청할 수 있으며, 분할 업로드·패키지 구성 검사·설치 전 무결성 검사를 적용합니다. 같은 버전 재설치도 기존 설치 폴더를 보존해 복구할 수 있습니다.
- Windows 업데이트에서 Dart 인증서 검증이 실패하면 Windows 인증서 저장소와 SSL 정책으로 검증 후 재시도합니다. 서버 주소·유효기간·신뢰·폐기 검사를 유지하고, Windows 검증은 UI와 별도로 실행합니다.
- 앱 설정과 WEB 관리자에서 리버스 포워딩 ID를 수동 고정할 수 있습니다. 배정이 중복 등으로 거절되면 `ysignage7-1`, `ysignage7-2`처럼 번호를 붙이고, 성공한 ID를 저장해 재시작·업데이트·설정 백업 복원 후에도 유지합니다. 중계 서버 호스트 이름 규칙의 접미사 지원 예시도 추가했습니다.
- 선택한 테마를 ID로 연결하여 업데이트 후 최신 테마 색상과 설정을 반영하고 직접 수정한 값은 별도 재정의로 유지합니다. 구버전에서 이번 전환을 처음 적용할 때는 기존 색상·UI 모양을 덮어쓰고 `high-contrast-text` 테마를 한 번 강제 적용합니다.
- 고대비 테마 두 종류의 버튼 배경을 조금 밝게 조정했습니다. 주제 선택 아이콘을 숨기는 `hideTopicIcons`와 주제별 `showIcon` 재정의, WEB 관리자 편집 항목을 추가했습니다.
- 기본 메뉴의 안내 이미지 항목, 메뉴 순서·문구·글꼴과 주제 선택 부제를 조정했습니다.

## 1.2.37 (2026-09-05)

- WebView 내부 링크와 메뉴 이동 중 동일한 로딩 팝업을 표시하고 취소·재시도를 제공합니다. 느린 사이트를 정상적으로 기다릴 수 있도록 메뉴 이동 응답 제한을 3초에서 12초로 늘렸습니다.
- Android 타사 쿠키를 허용하고 `webViewData.preserveCookies`에 `도메인|쿠키이름`을 지정해 화면보호기 진입 시 동의 쿠키만 선택적으로 보존할 수 있습니다. 기존 `preserveDomains`는 로그인 쿠키를 포함한 전체 도메인 보존 용도로 유지합니다.
- Windows에서 선택하지 않은 WebView를 일시 중지하고 다시 선택할 때 재개해 페이지·스크롤 상태를 유지하면서 백그라운드 메모리와 CPU 사용을 줄입니다.
- English 메뉴 글꼴을 가톨릭체로 통일하고 Google Maps 메뉴를 추가했으며, 언어 선택으로 돌아가는 문구를 짧은 `Languages`로 변경했습니다.

## 1.2.36 (2026-08-30)

- Windows에서 사이니지를 감춰도 앱이 생성한 WebView2 렌더러 입력 창이 화면에 남아 바탕화면 클릭을 가로채던 문제를 수정했습니다. 앱 창을 감추기 전에 모든 WebView2 컨트롤과 합성 표면을 숨기고, `ysignage.exe`와 전체 자식 프로세스가 만든 최상위 창도 `SW_HIDE`로 숨깁니다. 숨김 중 늦게 생성되는 WebView 컨트롤에도 같은 상태를 적용합니다. 트레이 메뉴는 숨은 앱 창을 foreground로 만들지 않고 키보드 훅도 완전히 해제하며, Explorer나 다른 프로그램의 입력·포커스는 제어하지 않습니다.

## 1.2.35 (2026-08-29)

- 언어를 선택한 뒤 표시되는 주제 선택 화면의 제목·부제와 **다른 언어 선택** 버튼을 선택 언어에 맞게 표시합니다. 한국어·영어·스페인어·프랑스어·포르투갈어·이탈리아어 기본 번역을 제공하며, 언어별 `topicSelectionTitle`, `topicSelectionSubtitle`, `changeLanguageLabel` 설정과 WEB 관리자 편집 항목으로 문구를 재정의할 수 있습니다.
- 서울특별시 홈페이지의 루트 페이지가 JavaScript로 실제 메인 페이지를 여는 과정에서 이전 탐색의 `CONNECTION_ABORTED`가 정상 페이지 위에 오류 화면을 표시하던 문제를 수정했습니다. 실패한 URL과 현재 URL을 비교해 교체된 탐색 오류를 무시하고, 리다이렉트 목적지가 로드되면 예약된 오류를 취소합니다. 기본 서울특별시 주소도 최종 메인 페이지로 직접 연결합니다.

## 1.2.34 (2026-08-29)

- Windows WebView2 통합을 소스 포함 플러그인으로 고정하고 잘못된 DevTools 이벤트 등록 인수를 수정했습니다. 리다이렉트 과정의 `CONNECTION_ABORTED`를 실제 접속 실패와 구분하며, JavaScript 하트비트가 늦을 때 렌더러 응답을 직접 확인한 뒤에만 WebView를 재생성해 서울시 홈페이지 등에서 간헐적으로 오류 화면이 나타나는 문제를 완화했습니다.
- UI 테마 밝기와 WebView의 `prefers-color-scheme`을 분리하고 WebView 기본값을 `light`로 설정했습니다. 첫 프레임과 메뉴 전환의 배경도 WebView 밝기를 따르며, 언어 선택 화면의 배경·버튼 크기·색상·글꼴을 테마와 WEB 관리자에서 설정할 수 있습니다.
- 메뉴 항목에 `showIcon` 재정의와 테마의 기본 아이콘 감춤을 추가했습니다. 확장자 없는 아이콘 경로는 밝기에 맞는 흰색·검정·컬러 파일을 자동 선택하고, 아이콘을 숨긴 버튼은 텍스트 크기를 동적으로 조절합니다. 버튼 문구는 줄바꿈할 때 단어를 우선 보존합니다.
- 메뉴 대상을 `url` 또는 `file` 중 하나로 지정할 수 있습니다. `file`은 확장자로 이미지·동영상·로컬 페이지를 자동 판별하며 `assets/...`, 업데이트 후 유지되는 `exdata/...`, 절대경로를 지원합니다. 이미지에는 WebView 밝기 기반 또는 사용자 지정 배경색, 핀치·버튼 확대/축소, 이동과 100% 복원을 제공합니다.
- 한국어·영어·스페인어·프랑스어·포르투갈어·이탈리아어별로 언어 선택 돌아가기 버튼과 툴팁을 표시하고, 긴 문구와 좁은 툴바용 짧은 문구를 설정에서 재정의할 수 있습니다.
- 전체 기본 글꼴을 가톨릭체로 정리하되 설정 화면은 Windows 기본 글꼴을 사용합니다. 박물관체, 서울남산체, 서울한강체와 라이선스를 추가하고 기본 테마를 고대비 텍스트 테마로 변경했습니다.
- 진단 정보에서 GitHub 로그인이나 추가 입력 없이 이슈를 직접 등록할 수 있는 중계 처리를 추가했습니다. 업데이트 확인은 인증 제한이 있는 GitHub API를 사용하지 않고 공개 Release 페이지만 사용합니다.
- 기본 키오스크 키 차단을 해제하고, 사이니지를 감춘 뒤 바탕화면을 클릭할 수 있도록 포커스를 반환합니다. Windows WebView 오류·렌더러 종료·페이지 응답 지연의 자동 복구와 진단 로그도 보강했습니다.

## 1.2.33 (2026-08-29)

- WEB 관리자에 사이니지가 배치된 모니터의 실제 데스크톱을 확인하는 저부하 실시간 미리보기를 추가합니다. 기본 2fps이며 1~5fps, 가로 640~1920px, JPEG 품질 20~80 범위에서 설정할 수 있습니다. 사이니지를 숨겨도 드러난 데스크톱 화면을 계속 갱신하고 숨김·최소화 상태를 텍스트로 표시하며, 첫 렌더링 전 빈 검은 프레임은 노출하지 않습니다.
- Windows 초기 실행에서 숨겨진 창을 먼저 전체화면 크기로 변경해 Flutter swapchain이 검은 화면에 머물 수 있던 문제를 수정했습니다. 정상 크기 창을 먼저 DWM에 표시한 뒤 전체화면으로 전환하고, 표시 직후와 초기 WebView 생성 구간에 렌더 표면을 단계적으로 다시 동기화합니다.
- 설정 재적용 시 기존 WEB 관리자 서버가 포트를 완전히 반환한 뒤 새 서버를 시작하며, Windows 데스크톱 미리보기의 GDI 비트맵 읽기 순서를 바로잡았습니다. 초기 렌더 복구 도중 오류가 발생해도 후속 복구를 예약하고, 사용자가 일시 정지한 미리보기는 브라우저 탭을 다시 열어도 자동 재개하지 않습니다.

## 1.2.32 (2026-08-29)

- WebView2가 페이지 로드 오류 뒤에 보낸 종료 이벤트를 성공으로 오인해 자동 재시도 타이머가 취소되던 문제를 수정했습니다. `페이지를 불러올 수 없습니다` 화면은 5초 후 자동으로 다시 시도하고, 3회 연속 실패하면 WebView를 재생성합니다.
- 사이니지 언어 선택 화면과 콘텐츠 화면에 현재 앱 버전을 작은 반투명 회색 오버레이로 표시합니다. 사이드 툴바에서는 하단 기능 버튼 아래에 기존 기능 버튼 배치를 거의 유지한 채 겹쳐 표시하고, 하단 툴바에서는 툴바 바로 위 우측, 상단 툴바에서는 화면 우측 하단에 배치됩니다.

- Windows 트레이 메뉴에 **웹관리자 열기**와 `리버스 포워딩: 연결됨/연결 안 됨` 상태를
  추가했습니다. 실제 포워딩 확인이 완료된 연결 상태이면 외부 접속 주소와 녹색 점등
  비트맵을 표시하고, 그 외 상태에는 회색 소등 비트맵을 표시합니다. Windows 기본 메뉴가
  컬러 이모지를 흑백으로 바꾸는 문제를 피하기 위해 네이티브 32비트 메뉴 아이콘을 사용합니다.
- 트레이 아이콘을 클릭하면 메뉴를 즉시 표시하도록 바꾸고 트레이 메뉴에 **사이니지 재시작** 항목을
  추가했습니다. 현재 트레이 플러그인이 네이티브 더블클릭 이벤트를 제공하지 않아
  더블클릭 동작은 임의의 시간 판별로 모방하지 않습니다.
- Windows 트레이 메뉴를 표시할 때 네이티브 메뉴 소유 창을 foreground로 지정해 메뉴
  바깥을 클릭하면 정상적으로 닫히도록 수정했습니다.
- Windows 앱 시작 시와 WEB 관리 포트·mDNS 설정 변경 시 Setup이 관리하는 방화벽
  상태를 자동 비교합니다. 규칙이 없거나 설정과 다를 때만 관리자 승인을 요청해 제한된
  규칙을 등록·교체하고, 관리 API를 끄면 기존 관리 규칙을 제거합니다.
- 이미 일치하는 규칙에는 PowerShell이나 UAC를 실행하지 않으며, 승인 처리 중 설정이 다시
  바뀌어도 후속 동기화를 순서대로 수행합니다. 승인 취소는 설정 저장이나 앱 실행을
  중단시키지 않고 다음 실행 또는 다음 설정 변경 때 다시 시도합니다.
- 앱에서 만든 규칙도 관리 마커를 기록해 이후 Setup 제거 시 함께 정리합니다. 포터블 ZIP은
  Setup 단계가 없으므로 첫 실행 시 규칙이 필요한 경우에만 승인을 요청합니다.

## 1.2.31 (2026-08-28)

- Setup에 **사설 네트워크에서 WEB 관리 자동 허용** 옵션을 기본으로 추가했습니다.
  관리자 승인을 한 번 받으면 현재 관리 TCP 포트와 mDNS UDP 5353을 도메인·사설
  네트워크의 같은 서브넷에만 허용해 최초 실행 시 Windows 방화벽 알림을 방지합니다.
- 방화벽 규칙은 버전별 실행 파일 경로와 무관하게 자동 업데이트 후에도 유지되며, 재설치에서
  옵션을 해제하거나 프로그램을 제거하면 Setup이 관리한 규칙만 삭제합니다. 규칙 생성이
  일부 실패하면 이미 만든 규칙과 임시 상태를 모두 되돌립니다.
- 공개 네트워크와 포터블·무인 설치에는 자동 허용을 적용하지 않으며, 관리 API나 mDNS가
  꺼진 기존 설정에서는 필요 없는 규칙을 만들지 않습니다.

## 1.2.30 (2026-08-28)

- Microsoft WebView2·Visual C++ 필수 설치 파일 다운로드가 일시적으로 끊겨도 서명을
  검증하면서 최대 3회 재시도하도록 Windows 릴리스 패키징을 보강했습니다.
- 1.2.28부터 포함된 키오스크 터치 안정화와 Windows 가장자리 스와이프 차단 변경을
  Windows 배포 자산으로 다시 게시합니다.

## 1.2.29 (2026-08-28)

- 1.2.28의 키오스크 터치 안정화와 Windows 가장자리 스와이프 차단 변경을 동일하게
  포함하며, 실패한 원격 Windows 패키징을 새 버전으로 다시 게시합니다.

## 1.2.28 (2026-08-28)

- 키오스크에서 메뉴를 빠르게 연속 터치할 때 준비 중인 불필요한 WebView를 정리하고,
  서로 다른 메뉴 선택 폭주와 같은 메뉴 재로드 반복을 제한해 화면이 응답하지 않는
  현상을 완화했습니다. 정상적인 같은 메뉴 더블 탭은 그대로 허용합니다.
- 포인터 이동에 따른 대기 타이머와 WebView 확대 상태 갱신을 병합하고, 누락된 Windows
  터치 종료 이벤트로 포인터가 누적되지 않도록 정리 로직을 보강했습니다.
- WebView 하트비트·페이지 응답 시간 초과를 진단 로그에 기록해 자동 복구 원인을 확인할
  수 있게 했습니다.
- Windows 전체화면 아래쪽 등 화면 가장자리에서 시작한 스와이프로 작업 표시줄이나 시스템
  UI가 열리지 않도록 차단했습니다. `windowsDisableEdgeSwipe` 설정으로 변경할 수 있으며
  기본값은 `true`이고, 사이니지를 감추거나 종료하면 자동 해제됩니다.

## 1.2.27 (2026-08-28)

- GitHub 비인증 REST API의 공인 IP당 시간당 60회 한도가 소진되어 403/429가 발생하면,
  API를 사용하지 않는 공개 최신 Release 리다이렉트와 직접 다운로드 경로로 자동
  전환하도록 수정했습니다.
- API 우회 중에도 Setup 보조 설치 파일을 SHA-256으로 검증할 수 있도록 업데이트
  매니페스트에 Setup 파일명과 해시를 포함했습니다.

## 1.2.26 (2026-08-28)

- 서울대교구와 GoodNews 툴바 아이콘의 불필요한 투명 여백을 제거하고, 선택 상태에서
  사용하는 컬러 아이콘을 추가했습니다. `goodnews-wite.png` 파일명 오타를
  `goodnews-white.png`로 바로잡고 모든 기본 메뉴 참조를 함께 정리했습니다.
- Vatican News의 교황 삼중관·교차 열쇠 심벌을 참고한 고해상도 투명 아이콘의
  컬러·흰색·검정 변형을 추가했습니다. 한국어·영어·스페인어·프랑스어·포르투갈어·
  이탈리아어 메뉴에 각 언어별 Vatican News 페이지를 연결했습니다.
- 한국어 `여의도동 성당` 주제에 연중행사, 성사안내, 미사안내 메뉴를 추가하고 성당
  홈페이지의 실제 안내 페이지와 연결했습니다.
- 폴더·포토 갤러리 대기화면의 기본 재생 순서를 무작위 재생으로 변경했습니다.
- Windows에서 앱을 다시 실행하면 기존 인스턴스를 트레이 숨김 상태와 관계없이 완전히
  종료하고, 종료 완료 후 새 인스턴스를 시작하도록 중복 실행 처리를 추가했습니다. 기존
  프로세스가 응답하지 않으면 해당 PID만 강제 종료한 뒤 새 인스턴스를 시작합니다.

## 1.2.25 (2026-08-28)

- `지금 설치`·F9·WEB 관리자 업데이트처럼 사용자가 명시적으로 요청한 수동 설치는
  누적 실패 3회 차단을 무시하고 재시도하도록 수정했습니다. 자동 업데이트는 기존 차단을
  유지합니다.
- 같은 버전의 자동 설치가 3회 실패해 차단되어도 첫 실패 묶음으로부터 24시간이
  지나면 실패 횟수와 시간 창을 초기화하고 정상 자동 설치 정책으로 다시 시도합니다.
- 수동 설치에서 기본 updater가 실패하면 실패 사유를 보여주고 사용자의 승인을 받은
  경우에만 동일 GitHub Release의 Setup EXE를 다운로드합니다. GitHub 자산 SHA-256을
  검증한 뒤 대화형 설치 프로그램을 실행하며, 자동 업데이트는 Setup을 자동 실행하지
  않습니다.
- 이전 대상 버전의 설치 실패 횟수가 새 업데이트 버전에 승계되어, 새 버전이 첫
  시도부터 `3회 실패` 차단을 받던 문제를 수정했습니다.
- 업데이터가 조기 종료할 때 종료 코드만 표시되던 문제를 개선해
  `update-state.json`의 실제 실패 사유를 함께 표시합니다.
- 설정 화면의 업데이트 상태 문구를 마우스로 선택·복사할 수 있게 바꾸고, `진단 정보로
  이슈 등록` 버튼이 진단 자료를 내보낸 뒤 원격 WEB 관리자의 진단·GitHub 이슈
  양식을 바로 열도록 연결했습니다.

## 1.2.24 (2026-08-28)

- WEB 관리자에 설치 폴더의 `exdata` 하위만 접근하는 탐색기 형태의 원격 파일 관리를 추가했습니다. 다중 선택·업로드·다운로드·폴더 생성·이름 변경·삭제를 지원하며, 현재 설정에서 사용하는 파일은 이름을 바꾸거나 삭제할 수 없도록 보호합니다.
- 레이아웃에서 UI 테마 설정을 분리하고 고대비·차분한 네이비·웜 스톤 기본 테마와 사용자 테마 저장을 지원합니다. 기존 설치에는 고대비 테마를 한 번만 적용하고, 이후 사용자가 선택하거나 편집한 테마는 업데이트 때 덮어쓰지 않습니다.
- 기본 설정과 사용자 변경분을 분리해 `menu.override.json`에는 실제로 바꾼 값만 저장하도록 개선했습니다. 기본값 변경은 업데이트에 반영하면서 사용자 설정은 유지되며, 기존 전체 복사 방식의 설정도 안전하게 축약합니다.
- 화면 보호기 게시판 주소마다 게시물 조회 기간과 최소·최대 게시물 수를 따로 지정할 수 있습니다. `gallery`, `signage1`, `movie` 등 서로 다른 게시판 구조를 공통 파싱하고, 제목이 `#`으로 시작하는 게시물은 제외합니다.
- 화면 보호기의 미디어 폴더와 슬라이드 목록에서 로컬·네트워크 동영상을 지원하고, 게시물·슬라이드·웹 화면 주소에 포함된 YouTube 영상은 소리와 함께 자동 재생합니다. 혼합 미디어 전환, 반복 재생, 시간 제한 및 썸네일 대체 처리도 추가했습니다.
- 개발 실행과 배포 패키지 모두에서 F1 사용자 매뉴얼이 열리도록 매뉴얼 렌더링을 통합하고, WebView에 입력 포커스가 있어도 F1 키를 앱으로 전달합니다.
- WEB 관리자에 운영체제·빌드 모드·업데이트 상태를 포함한 진단 정보와 이슈 리포트 기능을 추가했습니다. PHP-FPM 중계 서버가 GitHub App의 단기 설치 토큰을 사용해 로그인 없이 GitHub 이슈를 등록하며, 성공 시 생성된 이슈 번호와 주소를 표시합니다.
- WEB 관리자 역방향 포워딩의 임시 공용 키를 전용 RSA-3072 키로 교체하고, 내장 SSH 클라이언트가 배포 키를 정상 인식하는 회귀 테스트를 추가했습니다.
- 메뉴 항목에 선택 상태 전용 `selectedIcon`을 선택적으로 지정할 수 있게 했습니다. 값이 없으면 기본 아이콘을 그대로 사용하며, 사이드·가로 툴바와 WEB 관리자 편집·복원 화면에서 동일하게 동작합니다.
- 여의도성당 로고를 불필요한 여백 없이 자르고 주홍 계열 컬러·검정·흰색 자산을 추가했습니다. WYD 서울 2027 툴바 로고도 검정 배경과 밝은 배경에 맞는 그레이스케일 변형을 추가하고 기본 메뉴에 선택 상태 아이콘을 적용했습니다.

## 1.2.23 (2026-08-28)

- 외부 `ssh.exe` 없이 프로그램에 포함된 SSH 라이브러리와 전용 키를 사용해 WEB 관리자 역방향 포워딩을 연결하도록 추가했습니다.
- 원격 WEB 관리자 연결은 신규 설치와 기존 설정 마이그레이션에서 기본으로 켜지며, 명시적으로 끈 설정은 업데이트 후에도 유지됩니다.
- 사용 가능한 `ysignage{숫자}` ID를 자동으로 찾고 저장하며, 재연결 실패 시 ID를 다시 탐색하도록 했습니다.
- 설정 화면과 WEB 관리자에서 실제 포워딩 상태, 최근 확인 시각 및 접속 URL을 표시하고, 연결이 끊기면 주기적으로 재시도합니다.

## 1.2.22 (2026-08-26)

- 툴바 시작 위치에 언어 선택 화면으로 돌아가는 전용 버튼을 추가하고, 현재 주제 제목은
  클릭되지 않는 읽기 전용 라벨로 변경했습니다.
- WebView 뒤로·앞으로 버튼을 키보드·설정·화면 보호기·툴바 감추기 기능 버튼 묶음의
  가장 앞에 배치했습니다.
- 네비게이션 버튼과 기능 버튼 디자인을 개선하고, 사이드 툴바에서 기능 버튼 6개가
  충분한 크기를 유지하면서 3개씩 두 줄로 균형 있게 배치되도록 조정했습니다.

## 1.2.21 (2026-08-23)

- Windows 업데이트 실행기가 앱 종료 후 새 패키지의 `ysignage_updater.exe`를 실행 중인
  자기 자신 위에 덮어쓰면서 설치가 중단되던 문제를 수정했습니다.
- 네이티브 업데이트 실행기를 버전별 불변 경로인 `updater/payload`에 포함해 v1.2.19처럼
  기존 오류가 있는 실행기에서도 이번 릴리스를 설치할 수 있도록 했습니다.
- 실행 중인 업데이트 실행기와 대상 경로가 같은 경우 교체를 보류하는 방어 로직과 Windows
  경로 비교 회귀 테스트를 추가했습니다.

## 1.2.20 (2026-08-22)

- Windows 시작 시 첫 Flutter 프레임과 전체화면 전환이 경쟁해 입력은 처리되지만 검은 화면만
  남을 수 있던 창 표시 순서를 직렬화했습니다. 최초 표시·숨김 복원·디스플레이 복귀 시 실제
  렌더 표면 크기를 재동기화해 `ForceRedraw`만으로 복구되지 않던 상태를 처리합니다.
- Windows 키오스크 잠금을 전체 스위치와 개별 단축키 설정으로 세분화해 Windows 키,
  `Alt+Tab`, `Alt+Esc`, `Alt+F4`, `Alt+Space`, `Ctrl+Esc`, `Ctrl+Shift+Esc`,
  앱·메일·브라우저 실행 키의 차단 여부를 웹 관리자에서 각각 선택할 수 있습니다.
- UI 글꼴을 `layout.fontFamily`에서 지정하고 외부 `fonts` 폴더 → 패키지 글꼴 → Windows 시스템 글꼴 → Flutter 기본 글꼴 순서로 폴백하도록 했습니다.
- 나눔스퀘어, 나눔고딕, 나눔손글씨 붓, Pretendard 1.3.9, KoPub 돋움, 서울대교구 가톨릭체와 각 라이선스 안내를 설치·포터블·업데이트 패키지에 포함했습니다.
- 전체 UI, 툴바 전체·개별 메뉴, 언어 선택 화면 전체·개별 언어 버튼에 서로 다른 글꼴을 지정하고 개별 → 화면 전체 → 앱 전체 순서로 상속할 수 있습니다.
- 기본 메뉴 구성에 가독성 중심의 Pretendard를 기준 글꼴로 적용하고, 한국어 선택·성당·교구·공지·주보 항목에는 NanumSquare, Catholic, KoPubDotum, NanumBrush를 성격에 맞게 개별 지정했습니다.

- 툴바의 현재 주제 라벨을 누르면 언어 선택 화면으로 돌아가도록 변경했습니다.
- 현재 주제 라벨의 글자색을 설정할 수 있으며 기본색을 밝은 `#f8fafc`로 조정했습니다.
- 언어 선택과 주제 선택 버튼을 동일한 크기·모양으로 통일하고 터치 영역, 아이콘과 글자 크기를
  확대했습니다.

## 1.2.19 (2026-08-22)

- 관리자 PIN 인증 전에 표시하던 `웹 관리자 열기` 링크를 인증 후 설정 화면 하단으로 이동했습니다.
- Windows 사이니지가 보이는 동안 시스템 화면보호기와 화면 자동 끄기를 각각 방지할 수 있으며,
  두 설정은 기본적으로 사용하고 웹 관리자의 레이아웃 설정에서 개별 변경할 수 있습니다.
- 사이니지를 감추거나 종료하면 화면보호기·화면 끄기 방지를 즉시 해제해 Windows 전원 정책을
  다시 따르도록 했습니다.

## 1.2.18 (2026-08-22)

- 주제 선택 화면에서 화면보호기로 이동한 뒤 다시 깨우면 항상 언어 선택 첫 화면부터 시작하도록 수정했습니다.
- 현재 선택한 주제를 버튼과 구분되는 작은 상태 라벨로 툴바 시작 위치에 표시하며, 웹 관리자에서 표시 여부를 설정할 수 있습니다.
- 웹 관리자 로그인 유효시간을 마지막 활동 후 30분으로 지정하고 페이지 이동·입력·클릭·API 작업 중에는 자동 연장합니다. 설정 적용에 따른 관리 API 재생성에도 세션을 유지하며, 실제 인증 만료 시에는 편집 중인 설정을 유지한 채 PIN 재인증 오버레이를 표시하고 중단된 요청을 자동으로 다시 수행합니다.
- Windows 사이니지 표시 중 `Alt+Tab`, Windows 키 조합, 작업 관리자 등 앱 전환·셸 실행 단축키를 차단합니다. 사이니지를 감추면 잠금을 해제하며 웹 관리자의 레이아웃 설정에서 키 차단 여부와 일반 창보다 항상 위에 표시할지를 각각 변경할 수 있습니다.
- Windows 합성 표면이 투명해지는 현상에 대비해 불투명 창 배경, DWM·디스플레이·절전 복귀 시 표면 복구와 주기적 Flutter 프레임 감시를 추가했습니다. 먹통 시 화면의 왼쪽 위·오른쪽 위 모서리를 연속해서 동시에 8초간 누르거나 `Ctrl+Alt+Shift+F4`를 연속 3초간 눌러 네이티브 경로로 강제 종료할 수 있습니다.

- 업데이트 설치·압축 해제·버전 전환·재시작·정상 실행 확인·롤백을 독립된
  `ysignage_updater.exe`에서 처리해 업데이트 과정의 PowerShell 의존성을 제거했습니다.
- 네이티브 런처가 Authenticode 신뢰 상태와 인증서 지문을 직접 검증하며, 업데이트 ZIP의
  경로 이탈 항목과 심볼릭 링크를 설치 전에 거부합니다.
- 새 버전이 정상 실행되면 기존 `update.ps1`, `launcher.ps1`, `SimpleKiosk.cmd` 실행 진입점을
  삭제합니다. 구버전에서 최초 전환할 때 필요한 스크립트는 호환용 패키지에만 유지합니다.
- 관리 API가 실행 중이면 프로그램의 관리자 PIN 입력 창 하단에 `웹 관리자 열기` 링크를
  표시하고, 같은 PC의 실제 관리 포트를 기본 브라우저로 엽니다.
- 웹 관리자의 `외부 메뉴 설정`을 `사이니지 구성`으로 변경하고, 언어·주제·메뉴 항목별로
  표시 또는 숨김을 선택할 수 있습니다. 숨겨진 기본 항목은 다음 표시 항목으로 자동 대체됩니다.
- 설치·제거, 포터블 실행, 언어→주제→메뉴 스키마, 항목 숨김과 네이티브 업데이트 구조를
  기준으로 README와 사용자·운영·플랫폼별 가이드의 오래된 설명을 정리했습니다.
- 프로그램 시작 시 schemaVersion 1·구형 `items`·이전 웹 관리자의 단일 `default` 주제
  설정을 백업하고 현재 언어→주제→메뉴 구조로 자동 마이그레이션합니다.

## 1.2.17 (2026-08-22)

- 언어별 메뉴 구조를 `언어 → 주제 → 메뉴` 구조로 확장해 같은 언어에서도 목적별로
  서로 다른 툴바 메뉴와 시작 페이지를 사용할 수 있습니다.
- 언어를 선택하면 선택한 언어 버튼이 상단으로 이동하는 애니메이션과 함께 해당 언어의
  주제 버튼을 표시하고, 주제를 선택하면 그 주제의 메뉴 구성으로 진입합니다.
- 주제가 하나뿐인 언어는 주제 선택을 생략하고 바로 진입하며, 웹 관리자에서 이 동작을
  끄고 항상 주제 선택 화면을 표시할 수 있습니다.
- 기존 `languages[].items`와 `defaultMenu` 설정은 단일 기본 주제로 자동 변환해 계속
  사용할 수 있으며, WebView 상태 키도 언어·주제·메뉴 ID를 모두 사용합니다.
- 웹 관리자에서 언어별 주제 추가·삭제·기본 주제 지정과 주제별 메뉴 편집을 지원합니다.

## 1.2.16 (2026-08-16)

- 웹 관리자에서 전체 설정, 레이아웃·화면보호기·언어·언어별 메뉴 섹션, 개별 필드와
  기본 메뉴 ID가 일치하는 메뉴 항목을 각각 프로그램 기본값으로 복원할 수 있습니다.
- WebView 컨트롤러·준비·마운트 상태를 숫자 인덱스 대신 `언어 ID + 메뉴 ID`로 관리하고,
  세대가 지난 지연 콜백을 차단해 메뉴 순서 변경과 언어 전환 시 잘못 연결되지 않게 했습니다.
- 언어별 `defaultMenu`를 지정해 언어 선택 직후 표시할 메뉴를 정할 수 있으며, 생략하면
  이전처럼 첫 번째 메뉴를 사용합니다.
- 툴바의 언어 선택 아이콘으로 현재 WebView 상태를 유지한 채 언어 선택 화면으로
  돌아갈 수 있습니다.
- 메뉴가 많은 세로 툴바는 버튼 높이를 자동 축소하고 최소 높이로도 부족할 때만 스크롤바를
  표시합니다. 가로 툴바도 메뉴 영역만 스크롤하며 탐색·키보드·설정 버튼은 고정됩니다.
- 툴바를 WebView와 분리된 합성 레이어로 유지해 WebView 로딩 중에도 위치와 표시를 안정화했습니다.
- 처음 방문하는 메뉴를 누르면 선택 상태를 즉시 반영하고 기존 화면 위에 비차단 로딩
  오버레이를 표시합니다. 기본 문서가 화면에 표시 가능한 즉시 새 WebView로 전환하며,
  응답이 오래 걸리면 취소·다시 시도 기능을 제공합니다.
- 웹 관리자에 쿠키·캐시·Local Storage 공유 상태와 화면보호기 진입 시 데이터 유지·쿠키만
  삭제·사이트 데이터 전체 삭제 정책을 추가하고, 로그인 유지 도메인 예외를 지원합니다.
- 언어 선택 시 툴바 배치를 선택 화면 뒤에서 먼저 완료해 깜빡임을 줄이고, 최초
  한국어 선택에서 기존 WebView 컨트롤러가 유실되어 네비게이션이 사라지던 문제를 수정했습니다.
- 사이드 툴바가 하단 고정 버튼 높이를 중복 계산해 불필요하게 스크롤되던 문제를
  수정하고, WebView 준비 전부터 탐색 버튼 영역을 고정해 메뉴가 밀리거나 깜빡이지 않게 했습니다.
- Windows 트레이 아이콘의 툴팁과 컨텍스트 메뉴 상단에 현재 프로그램 버전을 표시합니다.
- 외부 국기 이미지를 앱 에셋으로 포함해 언어 선택 화면에 한국·미국·스페인·프랑스·
  포르투갈·이탈리아 국기를 인터넷 연결 없이 표시합니다.
- 영어 선택 아이콘은 미국 국기와 영국 국기를 대각선으로 잘라 겹친 이미지로 표시합니다.
- 가상 키보드 방식을 설정에서 `Windows 키보드`와 `내장 키보드` 중 선택할 수 있습니다.
- Windows 기본값은 반복 표시·감춤이 가능한 Windows 화상 키보드이며, 실행할 수 없는
  환경에서는 앱 내장 키보드로 자동 대체합니다.
- Flutter 설정 폼과 WebView의 입력 요소(iframe 포함)에 포커스하면 키보드를 자동으로
  표시하고, 실제 키보드 창 상태를 동기화해 토글 버튼을 반복 사용할 수 있습니다.
- 설정 화면에서 Windows 시작프로그램 등록 상태와 설치 경로 일치 여부를 확인하고,
  등록·삭제 및 `사이니지 표시`/`숨김(트레이)` 시작 모드를 선택할 수 있습니다.
- 실행 직후 화면보호기 진입과 Windows WebView 생성이 겹쳐 화면보호기가 비정상
  표시되던 시작 경합을 제거하고, 화면보호기를 최초 프레임부터 안정적으로 표시합니다.
- 언어 선택 화면에 화면보호기 복귀 버튼을 추가하고, 언어를 선택하지 않은 상태에서도
  설정된 무입력 시간이 지나면 자동으로 화면보호기로 돌아갑니다.
- 단일 슬라이드쇼와 미디어 폴더에서도 좌우 방향키 및 좌우 스와이프로 이전·다음
  항목을 이동할 수 있습니다.

## 1.2.15 (2026-08-16)

- 웹 관리자에서 메뉴·언어·툴바·관리 API·업데이트 정책을 하나의 JSON 파일로
  내보내고 가져올 수 있으며, 저장 전에 전체 설정을 검증하고 직전 설정 복원을 제공합니다.
- 언어 선택 버튼에 국기 이모지, Material 아이콘, 에셋 또는 웹 이미지 아이콘을 표시할 수 있습니다.
- 두 줄 툴바 제목이 공간에 맞도록 글꼴 크기를 자동 조정합니다.
- 프로그램·WebView·업데이트·관리 API 로그를 분리하고 파일 순환 및 30일 보존 정책을 적용합니다.
- 웹 관리자에서 분류별 로그와 시스템 정보를 포함한 진단 보고서를 다운로드하고
  GitHub 이슈 작성 화면으로 이동할 수 있습니다.
- 자동 업데이트 후에도 남아 있던 `Simple Kiosk` 바탕화면·시작프로그램 바로가기와
  시작 메뉴 그룹 이름을 `여의도성당Signage`로 변경합니다.

## 1.2.14 (2026-08-16)

- 실제 Windows 앱 실행 파일명을 `ysignage.exe`로 변경합니다. 1.2.11의 하드코딩된
  업데이트 검사를 통과시키기 위해 `simple_kiosk.exe`는 서명된 호환 복사본으로만 유지합니다.
- 1.2.11 등 기존 설치본이 대상 앱의 설정 스키마를 이유로 업데이트를 거부하던
  호환성 검사를 제거하고, 배포 manifest가 구버전 호환값을 유지하도록 수정합니다.
- Windows 설치본의 시작 메뉴·바탕화면·시작프로그램이 PowerShell 스크립트 대신
  네이티브 `ysignage_launcher.exe`를 직접 실행합니다. 실행기는 현재/이전 버전 선택과
  운영 데이터 경로 전달을 유지하며 콘솔 창이나 PowerShell 실행 정책에 의존하지 않습니다.

## 1.2.13 (2026-08-16)

- 프로그램 표시 이름을 `여의도성당Signage`로 변경합니다.
- 사용자 화면과 문서의 용어를 `사이니지`로 통일합니다.
- 웹 관리자의 레이아웃 설정에서 색상 선택기로 툴바 배경색을 변경할 수 있습니다.
- 오른쪽을 포함한 모든 툴바 배치에 감추기 버튼을 추가하고, 플로팅 컨트롤로 다시 표시할 수 있습니다. 사이니지 창 감추기는 `화면 보호기 더블클릭 → 툴바 감추기 더블클릭`을 5초 안에 연속해야 작동합니다.
- 관리 API를 같은 네트워크에서 기본 `ysignage.local`로 찾고 접속할 수 있도록 mDNS를 제공합니다.
- 기본 툴바를 화면 오른쪽에 항상 표시하고 자동 숨김을 해제합니다.
- 프로그램 정보와 Windows 패키지에서 제작자·이메일 정보를 제거하고 GitHub 저장소 주소만 유지합니다.
- 관리자 PIN 인증 창에 터치용 숫자 키패드와 전체·한 자리 삭제 기능을 추가합니다.
- 오른쪽 세로 툴바의 기능 아이콘을 폭에 맞춰 배치하여 모든 버튼이 표시되도록 수정합니다.
- 툴바에서 별도의 사이니지 감추기 버튼을 제거합니다.
- 툴바의 업데이트 관리 메뉴를 설정으로 변경하고 설정 화면 하단에 확인 절차가 있는 완전 종료 버튼을 추가합니다.
- 웹 관리자 메뉴 설정에 기본값과 오버라이드를 병합한 실제 적용 구성을 표시하고, 레이아웃·화면보호기·언어·메뉴 항목별 설정 UI와 고급 JSON 편집을 제공합니다.
- 웹 관리자 페이지의 실행 상태·관리 API·외부 메뉴 설정을 탭으로 분리하고, 선택값은 드롭다운 또는 체크박스로 제공하며 메뉴 항목 화면에서도 언어를 추가·삭제할 수 있습니다.

## 1.2.12 (2026-08-16)

- 화면보호기를 해제하면 터치하기 쉬운 대형 언어 선택 화면을 표시합니다.
- 설정에서 언어를 자유롭게 추가하고 각 언어마다 독립된 메뉴 항목·URL·아이콘을 구성할 수 있습니다.
- 한국어·영어·스페인어·프랑스어·포르투갈어·이탈리아어 기본 구성을 제공하며 기존 단일 `items` 설정과 schemaVersion 1 오버라이드를 계속 지원합니다.
- 관리 API 상태 응답에 현재 선택된 언어를 추가합니다.

## 1.2.11 (2026-08-15)

- 앱·트레이·installer 아이콘을 투명 배경의 Flutter 계열 하늘색으로 변경합니다.
- Visual C++ Runtime DLL을 앱 로컬로 포함해 새 Windows PC와 포터블 환경에서 별도 VC++ 설치 없이 실행할 수 있습니다.
- installer와 포터블 준비 도구가 Microsoft Visual C++ Redistributable을 자동 설치하거나 업데이트합니다.
- installer가 Microsoft Edge WebView2 Runtime 설치 여부를 검사하고 누락 시 Microsoft 서명 Evergreen Bootstrapper로 자동 설치합니다.
- 포터블 ZIP에 WebView2 설치용 `InstallPrerequisites.cmd`와 Microsoft 서명 검증 절차를 포함합니다.
- 패키징 시 WebView2 Bootstrapper의 Authenticode 게시자를 검증하고 검증 실패 시 빌드를 중단합니다.

## 1.2.10 (2026-08-15)

- 제공된 로고의 전면 성당 심볼을 앱, 작업 표시줄, 트레이, installer용 다중 해상도 아이콘으로 적용합니다.
- 관리자 PIN으로 인증하는 내장 관리 API와 반응형 관리자 웹 페이지를 제공합니다.
- 외부에서 실행 상태 조회, 메뉴 설정 변경, 보이기·감추기, 재시작, 완전 종료, 업데이트를 수행할 수 있습니다.
- 관리 API 포트는 설정 화면 또는 관리자 페이지에서 변경할 수 있으며 기본값은 80입니다.
- 로그인 세션 만료, 인증 실패 속도 제한, 요청 크기 제한과 브라우저 보안 헤더를 적용합니다.

## 1.2.9 (2026-08-15)

- F12 프로그램 정보에 GitHub 저장소 주소를 표시합니다.
- Windows EXE 속성과 installer에 GitHub 지원·업데이트 정보를 표시합니다.
- installer에서 Windows 로그인 시 자동 실행 등록 여부를 선택할 수 있으며 기본값은 등록입니다.
- Markdown 사용자 매뉴얼을 패키징 시 오프라인 HTML로 자동 변환해 installer와 포터블 ZIP에 포함합니다.
- 시작 메뉴의 사용자 매뉴얼 바로가기와 자동 업데이트 시 HTML 매뉴얼 동기화를 지원합니다.
- F1을 누르면 별도 프로세스 없이 프로그램 내부 WebView 팝업으로 사용자 매뉴얼을 표시합니다.
- Windows 알림 영역에 시스템 트레이 아이콘을 표시하고 보이기·감추기, 설정, 사용자 매뉴얼 메뉴를 제공합니다.
- 창 닫기와 메뉴의 감추기는 프로세스를 유지하며, 프로그램의 완전 종료는 트레이 메뉴에서 수행합니다.

## 1.2.8 (2026-08-15)

- 최초 실행만 SmartScreen 확인을 거치도록 Windows installer EXE 배포를 추가했습니다.
- 기본 설치 위치는 사용자별 `%LOCALAPPDATA%\Programs\SimpleKiosk`이며 관리자 권한이 필요하지 않습니다.
- installer가 버전 런처 구조와 시작 메뉴 바로가기를 구성해 이후 실행과 자동 업데이트를 설치 폴더 안에서 처리합니다.
- installer가 Windows 시작프로그램을 등록하며, 기존 설치 경로와 이전 시작프로그램 바로가기를 확인해 설치 위치 변경 시 새 경로로 갱신합니다.
- ZIP의 `simple_kiosk.exe` 직접 실행과 포터블 폴더 기반 런처 설치를 계속 지원합니다.
- Windows 제거 프로그램을 지원하며 앱 본체와 시작프로그램 등록은 항상 제거합니다.
- 제거 시 설정·미디어·상태·로그 등 사용자 데이터 삭제 여부를 선택할 수 있고 기본값은 보존입니다.
- GitHub Release에 자동 업데이트 ZIP과 최초 설치용 installer EXE를 함께 게시합니다.

## 1.2.7 (2026-08-15)

- 관리자 기본 PIN을 `1259`로 제공합니다.
- 관리자 화면에서 PIN을 변경할 수 있으며, 변경 PIN은 별도의 PBKDF2 솔트 해시 파일에 저장합니다.
- 관리자 PIN 파일을 삭제하면 기본 PIN `1259`로 복원됩니다.

## 1.2.6 (2026-08-15)

- WebView2가 포커스를 가진 상태에서도 Windows 네이티브 키 훅으로 `F9`, `F12`를 처리합니다.
- 업데이트 실행기 시작을 확인한 뒤에만 앱을 종료하여 조용히 종료되는 실패를 방지합니다.
- 업데이트 후 첫 실행 전에 런처를 설치해 포터블 직접 실행 환경도 자동 복구합니다.
- Windows 설정, 로그, 다운로드 및 버전의 기본 루트를 `simple_kiosk.exe`가 있는 폴더로 변경합니다.
- 별도 데이터 루트는 `SIMPLE_KIOSK_DATA_DIR`을 명시한 경우에만 사용합니다.

## 1.2.5 (2026-08-13)

- `F12`를 누르면 앱과 Updater 버전 정보를 표시합니다.
- `F9`를 누르면 즉시 업데이트를 확인하고, 사용자 동의 후 다운로드·설치·재시작합니다.

## 1.2.4 (2026-08-13)

- 업데이트 manifest와 설정 스키마의 호환성을 설치 전에 검사합니다.
- manifest가 요구하는 최소 Updater 버전보다 낮은 장비에서는 설치를 차단합니다.
- 코드 서명 배포본은 설치 시 Authenticode 신뢰 체인과 인증서 지문을 검증합니다.

## 1.2.3 (2026-08-13)

- Windows 자동 업데이트 정책을 관리자 화면에서 상세 조정할 수 있습니다.
- 업데이트 성공 후 이전 버전, 다운로드와 로그를 보관 정책에 따라 정리합니다.
- 고정 업데이터 동기화, 수동 복구와 진단 ZIP 내보내기를 추가했습니다.
- GitHub Actions에서 선택적으로 Windows 실행 파일을 코드 서명하고 검증합니다.

## 1.1.0+2 (2026-08-09)

하단 툴바의 사이니지 운용성과 Windows 배포 절차를 개선한 기능 릴리즈입니다.

### 새로운 기능

- 하단 툴바를 숨기고 최소 오버레이 컨트롤로 복원할 수 있습니다.
- 숨김 오버레이를 화면 안에서 드래그하고 가까운 모서리에 배치할 수 있습니다.
- `layout.toolbarInitiallyHidden`으로 시작 시 툴바 숨김 여부를 설정할 수 있습니다.
- `layout.toolbarAutoHideSec`으로 펼친 툴바의 자동 숨김 시간을 설정할 수 있습니다. `0`은 자동 숨김 비활성화입니다.
- Windows, Android, macOS용 릴리즈 패키징 스크립트와 GitHub Actions 워크플로를 추가했습니다.
- 배포 패키지에 플랫폼별 설치 가이드와 메뉴 설정 가이드를 포함합니다.

### 개선 사항

- 툴바를 숨기거나 다시 표시해도 WebView 위젯과 현재 페이지 상태가 유지됩니다.
- 오버레이에서 뒤로, 앞으로, 툴바 복원, 가상 키보드 제어를 계속 사용할 수 있습니다.
- Windows 최초 설치, WebView2 Runtime, 무결성 확인 및 업데이트 절차를 문서화했습니다.
- 툴바 숨김·복원, 드래그, 자동 숨김 및 상태 유지 동작에 대한 위젯 테스트를 보강했습니다.

### 설정 및 업그레이드 참고

- 기본 설정은 `toolbarInitiallyHidden: true`, `toolbarAutoHideSec: 10`입니다.
- 기존처럼 툴바를 항상 펼쳐 두려면 `assets/config/menu.json`의 `layout`에 다음 값을 지정하세요.

```json
{
  "toolbarInitiallyHidden": false,
  "toolbarAutoHideSec": 0
}
```

- 기존 설치 폴더에 일부 파일만 덮어쓰지 말고 새 ZIP을 별도 폴더에 완전히 압축 해제하세요.
- 운영 중 수정한 `menu.json`과 사용자 이미지는 백업 후 새 배포본으로 옮기세요.

## 1.0.0+1

- Flutter 기반 Windows/Android/macOS 사이니지 앱 최초 릴리즈.
- JSON 메뉴 설정, 다중 WebView 상태 유지, 대기화면, 자동 복구 및 가상 키보드 기능 제공.
