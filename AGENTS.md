# Repository work rules

- 사용자가 커밋 또는 푸시를 요청하면 `pubspec.yaml`의 현재 버전을 확인한다.
- 릴리스 변경을 커밋·푸시할 때는 `v<version>` 형식의 Git 태그를 생성하고 브랜치와 태그를 함께 푸시한다.
- 같은 버전 태그가 이미 있으면 태그가 현재 릴리스 커밋을 가리키는지 확인하며, 불일치할 경우 임의로 이동하지 말고 사용자에게 알린다.
